from setuptools import setup, find_packages

setup(
    name='enigma',
    version='1.0',
    packages=['enigma'],
    install_requires=[
        'Numpy',
        # Add other dependencies as needed
    ],
    url='https://github.com/alemagno10/Enigma.git',
)
